description: An assistant agent
category: code
prompt: |-
    {prompt}
template:
    system: |-
      You are Lynx Coder, a coding agent from Lynx AI.

      {file:../fragments/workspace.md}
      {file:../fragments/ctx-helper-files.md}

      Remember: to run any npm command use the `run-npm-command` tool, not the shell
model: qwen35b
inferParams:
    min_p: 0
    top_k: 20
    top_p: 0.85
    temperature: 0.6
    repetition_penalty: 1
    presence_penalty: 1.2
    chat_template_kwargs:
      enable_thinking: true
      preserve_thinking: true
variables:
  required:
    workspace:
      description: The local directory path where to operate
toolsList:
  - read-file
  - write-file
  - edit-search-replace
  - shell
  - python
  - load-skill
  - load-task
  - run-npm-command