{
  "name": "lynx-coder",
  "version": "0.0.2",
  "description": "Lynx IA coding agents",
  "repository": "https://github.com/lynxai-team/lynx-coder",
  "scripts": {
    "build": "cp -r dist/. dist-backup/ && rm -rf dist-backup/actions && tsc && cp -r dist-backup/* dist/ && rm -rf dist-backup/"
  },
  "dependencies": {
    "@agent-smith/core": "^0.0.15",
    "@boxlite-ai/boxlite": "^0.9.7",
    "ansi-colors": "^4.1.3"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "tslib": "^2.8.1",
    "typescript": "^7.0.2"
  },
  "module": "./dist/main.js",
  "type": "module",
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public",
    "registry": "https://registry.npmjs.org/"
  },
  "license": "MIT"
}